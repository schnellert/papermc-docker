name: Synchronize Velocity

on:
  schedule:
    - cron: '0 0 * * FRI'
  workflow_dispatch:
    inputs:
      force:
        default: false
        description: Force build all Velocity images
        type: boolean

concurrency:
  cancel-in-progress: true
  group: ${{ github.workflow }}-${{ github.ref }}

jobs:
  synchronize:
    secrets: inherit
    uses: ./.github/workflows/synchronize.yml
    with:
      force: ${{ inputs.force || false }}
      project: velocity
